#!/usr/bin/python3
def weight_average(my_list=[]):
    if len(my_list) is None:
        return 0

        total = sum(map(lambda x: x[1], my_list))
        weighted = sum(map(lambda x: x[0] * x[1], my_list))

       return  weighted / total
