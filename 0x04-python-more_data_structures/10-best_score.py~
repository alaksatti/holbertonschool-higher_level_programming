#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary is None:
        return None
    max = None
    for keys, values in a_dictionary.items():
        if values > max:
            maxkey = keys
    return maxkey
